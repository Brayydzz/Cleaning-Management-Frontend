describe("test submit booking form", () => {
  it("asd", () => {
    expect(true).to.be(false);
  });
  // context("testing booking form"),
  //   () => {
  //     beforeEach(() => {
  //       // cy.intercept
  //     });
  //   };
  // it("input text in submit form"),
  //   () => {
  //     cy.intercept(
  //       {
  //         url: "http//:localhost:3001/bookings",
  //         method: "POST",
  //       },
  //       { statusCode: 201 }
  //     );
  //     cy.visit("http//:localhost:3001/bookings");
  //     cy.get("form").within(() => {
  //       cy.get("input:first").type("hello");
  //       cy.get("form:nth-child(2) > input:first").type("hello");
  //     });
  //     cy.get("button").click();
  //   };
});
